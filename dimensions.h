#ifndef DIMENSIONS_H
#define DIMENSIONS_H

  
const float FramesPerSecond = .5;
const int ColCount = 8;
const int RowCount = 8;
const int SlicesPerFrame = 64; // Hz
const unsigned long MicrosPerFrame = 1000000 / FramesPerSecond; // µs


#endif

